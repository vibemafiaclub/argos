# QA — 2026-05-14-skills-project-breakdown

## 변경 범위 (git diff stat)

```
 docs/adr.md                                                   | 233 +++++
 packages/shared/src/types/dashboard.ts                        |  27 +
 packages/web/src/app/api/orgs/[orgSlug]/dashboard/skills/route.ts | 81 ++-
 packages/web/src/app/dashboard/[orgSlug]/skills/page.tsx       |  30 +-
 packages/web/src/lib/server/dashboard-row-mapping.test.ts      |  51 +
 packages/web/src/lib/server/dashboard-row-mapping.ts           |  44 +-
 packages/web/src/components/dashboard/skill-projects-cell.tsx  | NEW (197 LOC)
 packages/web/src/components/ui/popover.tsx                     | NEW (58 LOC)
 6 modified + 2 new files, 456 insertions / 10 deletions
```

(`git diff HEAD --stat` + untracked: `skill-projects-cell.tsx`, `popover.tsx`)

## 자동 검증 결과

| # | 명령 | 결과 | 비고 |
|---|------|------|------|
| A1 | `pnpm --filter @argos/shared build` | pass | tsc 통과 |
| A2 | `pnpm --filter @argos/web typecheck` | pass | tsc --noEmit 통과 |
| A3 | `pnpm --filter @argos/web test src/lib/server/dashboard-row-mapping.test.ts` | pass | 8/8 (mapSkillRow 신규 5 케이스 + 기존 3) |
| A4 | `pnpm --filter @argos/web lint` | pass | 사전 존재 warning 1건(`settings/projects/page.tsx` `session unused`) — 변경분 무관 |
| A5 | `pnpm --filter @argos/web build` | pass | Next.js production build 성공, `/dashboard/[orgSlug]/skills` 9.47 kB |

## QA 시나리오 결과

| # | 시나리오 | 결과 | 심각도 | 메모 |
|---|---------|------|--------|------|
| S1 | dev 서버(`pnpm --filter @argos/web dev`) 기동 | pass | - | port 3000, Ready in 1277ms |
| S2 | `/api/.../skills` 응답에 `projects` / `additionalProjectCount` 키 존재 | pass | - | 모든 14개 skill 행에 두 필드 모두 채워짐. `lastUsedAt`은 ISO8601 UTC(.MS 자리수 포함) |
| S3 | Top 5 + `(+N more)` 분포 — 7-project skill 시드(100/80/60/40/20/10/5) | pass | - | `additionalProjectCount=2`, projects.length=5, invocations 내림차순 |
| S4 | 호버 팝오버 — Top 5 분포 + lastUsedAt | pass | - | 컴포넌트 코드 검토: `Popover.Trigger openOnHover delay=300 closeDelay=150`, popup 내 invocations 막대 + `formatLastUsed` 표시. 자동 driver 부재로 코드 기반 검증 |
| S5 | 셀 내 project name 클릭 → URL `?projectId=` 교체 + 재페치 | pass | - | API 응답 검증: `?projectId=cmo110dgl0007nk01sdoxtjbt` (argos) 호출 시 모든 skill의 projects.length=1, additionalProjectCount=0. 클라이언트 핸들러 `setProjectIdQuery`는 sessions/page.tsx 의 setQuery 패턴 그대로 사용 |
| S6 | `?projectId=` 활성 상태 셀 disabled | pass | - | `SkillProjectsCell` 의 `isProjectFiltered=true` 분기: `aria-disabled, disabled, opacity-60 cursor-default` 적용된 단일 button 렌더 |
| S7 | 팝오버 내 project name 클릭 (확장 8a) | pass | - | `ProjectBreakdownPopup` 의 onClick → `setOpen(false)` 후 동일 `onSelectProject` 호출 (R1 통일 핸들러) |
| S8 | project 0개 사용자 빈 상태 (확장 3a) | pass | - | 페이지의 `skills.length === 0` 빈 상태 분기 그대로 유지(변경 없음). API 응답 `{skills: []}` 시 빈 카드 노출 |
| S9 | skill의 project 0개 (확장 6a) — `—` 표시 | pass | - | `SkillProjectsCell`의 `projects.length===0` → `<span class="text-muted-foreground">—</span>`. mapSkillRow 단위테스트(`projects_json=null`, `잘못된 모양` 케이스) 가 폴백 동작 보증 |
| S10 | API 5xx 시 에러 UI | pass | - | `error` state에서 Alert + 재시도 버튼. 부분 컬럼 손상 없음 (기존 흐름, 변경 무관) |
| S11 | 권한 일관성 (G4·M2) | pass | - | 다른 org 슬러그(`khronos`) JWT 호출 → 403. `projectId=does-not-exist` → 404. CTE 가 `JOIN projects p ON p.org_id=access.org.id` + `WHERE sc.project_id = ANY(projectIds)` 다중 가드 |
| S12 | 결정적 tiebreaker (R2) | pass | - | 동률 invocations=5 인 3 project (argos / vibemafiaclub-hub / vooster-ai) 시드 → 같은 호출 2회 동일 순서 (`project_name ASC, project_id ASC`) |
| S13 | 페이로드 회귀 (R3) | pass | - | 기준 측정: baseline median **0.312s / 2393B** vs post **0.331s / 6135B** → **1.06× ≤ 1.20×**, payload 6135B << 40KB. 둘 다 임계 통과 |
| S14 | 키보드 접근성 | pass (코드 검토) | - | 각 project 텍스트 `<button>` `aria-label="Filter skills by project <name>"`, 팝오버 트리거 `aria-label="Show project breakdown"`, base-ui 기본 Escape/포커스 트랩 |
| S15 | 모바일/터치 | pass (코드 검토) | - | 트리거 click/focus 가 기본. `openOnHover`는 데스크탑 보강만. 탭으로 동일 동작 |
| S16 | Escape / Outside click 닫힘 | pass (코드 검토) | - | base-ui 1.4 `Popover.Popup` 기본 동작 + `modal={false}` 명시 |

총괄: 16/16 pass (그중 S4/S14/S15/S16은 헤드리스 브라우저 driver 부재로 코드 검토 기반). critical 0, major 0, minor 0, nit 0.

## 발견 이슈 (사용자 반영 선택용)

없음.

(추가 관찰 — 이슈 아님)

- 셀 텍스트 영역의 `truncate` 와 inline `<button>` `whitespace-nowrap`이 결합되어 좁은 viewport(≤320px)에서는 첫 project name 도 잘릴 수 있다. 단 풀 텍스트는 팝오버에서 제공되므로 Decision-4(데이터 손실 없음) 와 일치. 정보 손실 없음.
- 실제 운영 데이터(`sumin-choi` org)에서는 skill 당 project 수가 1–2개에 그쳐 `(+N more)` 가 자연스럽게 노출될 케이스가 거의 없음. QA 는 시드 데이터로 보강하여 검증함.

## 검증 환경

- 명령: `pnpm --filter @argos/web dev` (port 3000)
- 인증: JWT (sumin-choi org OWNER 사용자 `cmnyt8zqs0003ms01nx1r9m74`) + 임시 CliToken 레코드. 검증 후 모두 삭제.
- 시드 데이터:
  - `qa-multi-project-demo` skill 7 project 분포 (100/80/60/40/20/10/5 invocations) → 315 events
  - `qa-tiebreaker-demo` skill 3 project 동률 (각 5 invocations) → 15 events
  - 검증 종료 후 `event.deleteMany({ id in [...] })` 로 330 row 모두 제거
- 비교용 baseline: `git stash`로 작업트리 일시 복원 후 동일 호출 10회 측정 → 다시 `git stash pop`
- 도구: curl (`-w "time_total / size_download"`), python3 median, prisma client (직접 DB 조회)
- 사후 정리: 시드 이벤트 삭제, 테스트용 CliToken 삭제, 복사한 `.env.local` 제거, `/tmp/qa-*` 파일 제거
