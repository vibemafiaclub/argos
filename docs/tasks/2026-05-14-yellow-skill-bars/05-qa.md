# QA — 2026-05-14-yellow-skill-bars

## 변경 범위 (git diff stat)

```
 packages/web/src/components/dashboard/session-activity-ribbon.tsx | 16 +----
 packages/web/src/components/dashboard/session-ribbon-visuals.ts   | 19 +++++  (신규)
 packages/web/src/components/dashboard/session-ribbon-visuals.test.ts | 84 ++++  (신규)
 packages/web/src/lib/timeline-events.test.ts                      | 70 ++++++++++++++++-
```

ribbon `.tsx` 의 내부 `segmentVisuals` 함수가 통째로 삭제되고 `./session-ribbon-visuals` 의 named import 로 교체됨. 새 helper 는 pure TS (type-only import 만 사용). 기존 머지바 head (`bg-muted-foreground`, line 291) 는 변경되지 않음.

## 시나리오 결과

| # | 시나리오 | 결과 | 심각도 | 메모 |
|---|---------|------|--------|------|
| 1 | (단위) skill 호출 막대 → `bg-chart-4` | pass | - | `pnpm test` case 1 통과 |
| 2 | (단위) subagent 호출 막대 → `bg-chart-4` | pass | - | case 2 통과 |
| 3 | (단위) skill + subagent 동시 → `bg-chart-4` | pass | - | case 3 통과 (OR 가드) |
| 4 | (단위) 일반 tool (Bash) → `bg-muted-foreground` | pass | - | case 4 통과 |
| 5 | (단위) HUMAN message → `bg-brand` flex `0 0 3px` | pass | - | case 5 통과 |
| 6 | (단위) ASSISTANT message (outputTokens=100) → `bg-brand-2` flex `100 0 6px` | pass | - | case 6 통과 |
| 7 | (단위) ASSISTANT message (outputTokens=0) → grow clamp 1 | pass | - | case 7 통과 |
| 8 | (회귀) skill/subagent 가 동일 toolName 연속 호출 중에도 머지 그룹에서 제외됨 | pass | - | timeline-events.test.ts 신규 케이스 통과 |
| 9 | (정적) ribbon line 291 의 머지바 head 가 `bg-muted-foreground` 유지 | pass | - | grep 확인. 머지바 head 변경 0 |
| 10 | (정적) `session-ribbon-visuals` 외부 import 경계 | pass | - | grep 결과 ribbon `.tsx` + 자기 자신 + 테스트 3 파일만 |
| 11 | (정적) event-list / event-detail 의 `isSpecial ? "bg-chart-4" : "bg-muted-foreground"` 와 helper 분기 의미적 일치 | pass | - | event-list line 138, event-detail line 84 와 동일 분류 키 |
| 12 | 전체 vitest (67 tests) | pass | - | 6 files, 67 tests pass |
| 13 | typecheck (`pnpm typecheck`) | pass | - | `@argos/shared` 빌드 후 통과 |
| 14 | lint (`pnpm lint`) | pass | - | 0 error, 1 warning (무관) |
| 15 | next build (`pnpm build`) | pass | - | dummy env 로 production build 성공 |
| 16 | 실제 dashboard transcript 페이지에서 (a)(b) 노랑 / (c) 회색 / (d) 회색(머지바) 시각 회귀 | blocked | - | 로컬 `.env` 부재로 DB 접속 불가. dev server 자체는 정상 부팅, login page 200 응답. 자동 7 케이스 + 정적 grep + event-list/detail 의미 일치로 시각 결과 간접 확인. |
| 17 | 다크 모드 토글 후 동일 검증 | blocked | - | 16 과 동일 사유 |
| 18 | 호버/선택 상태 회귀 점검 | blocked | - | 16 과 동일 사유. negative space (ribbon `.tsx` 의 호버/선택/outline 라인) 는 git diff 상 변경 없음으로 정적 확인. |

총괄: pass 15, blocked 3 (모두 실제 DB-backed 시각 확인 — 환경 한계), fail 0.

## 발견 이슈 (사용자 반영 선택용)

없음.

부가 관찰 (이슈 아님):
- helper 파일은 type-only import 만 사용 → `isolatedModules` 하에서 값 오용시 즉시 컴파일 에러 처리. plan Decision-3 보강 근거대로 동작.
- ribbon `.tsx` 에서 `CSSProperties` import 가 제거됐고 helper 로 옮겨짐 (사용처 없으므로 정상).
- 시나리오 16~18 의 시각 회귀는 자동 단위 테스트가 `bg-chart-4` / `bg-muted-foreground` / `bg-brand` / `bg-brand-2` 라는 정확한 클래스 문자열을 단언하고, ribbon `.tsx` 가 이 문자열을 그대로 `className` 에 주입함이 git diff 로 확인되므로 (다크 모드 토큰은 `globals.css` line 156 에 정의되어 있음) 시각 결과의 회귀 위험은 극히 낮다. 다만 머지바 head 클래스의 직접적인 DOM 단언은 본 task 범위 밖.

## 검증 환경
- 명령: `pnpm --filter @argos/web test` (vitest), `pnpm --filter @argos/web typecheck`, `pnpm --filter @argos/web lint`, `pnpm --filter @argos/web build`
- dev server: `pnpm --filter @argos/web dev` (dummy env 로 부팅 확인, port 3000). 로그: `/tmp/argos-dev.log`
- 브라우저: 사용 안 함 (curl 로 응답 코드 확인). 실제 DB credential 부재로 dashboard 진입 불가.
- 시드 데이터: 없음. helper 단위 테스트의 `makeTool` / `makeMessage` 픽스처가 6 종 fixture 조건 (a)~(f) 중 (a)(b)(c)(d)(e) 를 모두 커버하고, (f) 동일 toolName 연속 호출의 머지 제외 가드는 `timeline-events.test.ts` 의 신규 케이스로 커버됨.
- 머지바 head 의 `bg-muted-foreground` 유지는 ribbon `.tsx` 의 line 291 (plan 의 "line 303" 은 함수 삭제 전 기준; 삭제 후 +12 시프트) grep + git diff 비변경 확인으로 가드.
